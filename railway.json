{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd noc-qms-app && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd noc-qms-app && NODE_ENV=production node server.cjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
