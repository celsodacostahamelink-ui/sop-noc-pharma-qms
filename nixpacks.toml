[phases.install]
cmds = ["npm install --prefix noc-qms-app"]

[phases.build]
cmds = ["npm run build --prefix noc-qms-app"]

[start]
cmd = "node noc-qms-app/server.cjs"

[variables]
NIXPACKS_NO_CACHE = "1"
